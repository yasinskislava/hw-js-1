// Створення почти з імені й фамілії0

const name = prompt("Введіть ім'я");
const surname = prompt("Введіть фамілію");

const email = name.concat(surname).toLowerCase() + "@gmail.com";
console.log(email); 

