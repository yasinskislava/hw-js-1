const isOnline = true;
const isFriend = true;
const isDnd = false;
if (isOnline === true && isFriend === true && isDnd != true) {
    console.log("Ви можете відкрити чат з цим користувачем");
}
else {
    console.log("Ви не можете відкрити чат з цим користувачем");
}